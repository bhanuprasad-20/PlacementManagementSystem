<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Student_Required</fullName>
    <active>true</active>
    <description>Prevents saving an Application when Student is empty.</description>
    <errorConditionFormula>ISBLANK(Student__c)</errorConditionFormula>
    <errorMessage>Student is required. Please select a Student before saving the Application.</errorMessage>
</ValidationRule>
